# Dependências da suíte de testes da raiz (unit, security e os checks de documento).
# O backend traz as suas próprias em backend/pyproject.toml — este arquivo é o que
# permite rodar os testes sem ter o projeto de produto instalado.
#
#   pip install -r tests/requirements.txt

pytest>=8.0
pyyaml>=6.0
jsonschema>=4.21
